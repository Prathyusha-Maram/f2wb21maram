extends layout

block content
  h1= title
  p Welcome to #{title}
  p "Polar bears have black skin. And actually, their fur isn't white—it's see-through, so it appears white as it reflects light."
  button(type = "submit", class = "btn btn-info btn-sm" onclick = 'doBump()') Press Me 
  div(id = "bump") Bump the count


  script.
      var count = 0
      var bump = 1
      function doBump(){
        count = count + bump;
        bump = bump + 1;
        document.getElementById('bump').textContent = 'Count '+count+' bump '+bump;
        document.getElementsByClassName('btn').textContent = "Hello"
      }